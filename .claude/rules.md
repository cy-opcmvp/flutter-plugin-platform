# Claude Code 编码规则

> 🤖 本项目遵循的编码规范和文件组织规则

## 📚 项目理解

**开始任何任务前，请先阅读**:
- [项目概览](PROJECT_OVERVIEW.md) - **必读** - 项目定位、架构、技术栈
- [.kiro/specs/](../.kiro/specs/) - 技术规范目录
  - [插件平台设计](../.kiro/specs/plugin-platform/design.md)
  - [平台服务架构](../.kiro/specs/platform-services/design.md)
  - [外部插件系统](../.kiro/specs/external-plugin-system/design.md)

## 📋 核心规则

### 1. 文件组织规范

**必须遵守**: [文件组织规范](rules/FILE_ORGANIZATION_RULES.md)

**核心原则**:
- ✅ 根目录保持简洁，只保留最核心的文件
- ✅ 所有脚本放入 `scripts/` 目录
- ✅ 所有详细文档放入 `docs/` 目录
- ✅ 所有技术规范放入 `.kiro/specs/` 目录

**根目录只允许**:
- `README.md` - 项目主文档
- `CHANGELOG.md` - 版本变更日志
- `pubspec.yaml` - Flutter 配置
- 用户直接运行的设置脚本（如 `setup-cli.bat`）
- 核心功能快速入口（如 `PLATFORM_SERVICES.md`）

**禁止在根目录**:
- ❌ 临时修复脚本（`fix-xxx.ps1`）
- ❌ 临时文档（`*_FIX.md`）
- ❌ 实施报告（`FIXES_SUMMARY.md`）
- ❌ 插件详细文档（`WORLD_CLOCK_IMPLEMENTATION.md`）

### 2. 代码规范

#### Dart/Flutter 代码

- 遵循 [Effective Dart](https://dart.dev/guides/language/effective-dart) 指南
- 使用 `flutter_lints` 包进行代码检查
- 所有公共 API 必须有文档注释
- 使用类型注解，避免 `var` 和 `dynamic`
- 优先使用 `const` 构造函数
- 使用 `async/await` 而非 `.then()`

#### 文件和目录命名

- **文件名**: `snake_case.dart` (如 `audio_service.dart`)
- **类名**: `PascalCase` (如 `AudioServiceImpl`)
- **变量/方法**: `camelCase` (如 `getNotification()`)
- **常量**: `lowerCamelCase` 或 `UPPER_SNAKE_CASE`
- **私有成员**: 前缀下划线 `_privateMethod`

### 3. 插件开发规范

#### 插件 ID 命名

**格式**: `{domain}.{category}.{plugin-name}`

**规则**:
- 使用**小写字母**和**点号**
- **禁止使用下划线**和连字符
- 反向域名格式

**示例**:
```
✅ com.example.calculator
✅ org.company.tools.texteditor
❌ com.example.text_editor (有下划线)
❌ com.example.world-clock (有连字符，应为 worldclock)
```

#### 插件文档结构

每个插件的文档必须放在：
```
docs/plugins/{plugin-name}/
├── README.md              # 插件概述
├── IMPLEMENTATION.md      # 实现文档
└── UPDATE_v{version}.md  # 版本更新说明
```

插件代码目录的 `README.md` 应该简短，指向详细文档。

### 4. 服务开发规范

#### 服务接口定义

- 所有服务必须先定义接口 (`I{ServiceName}`)
- 接口放在 `lib/core/interfaces/services/`
- 实现放在 `lib/core/services/{service-name}/`

#### 服务生命周期

```dart
abstract class I{ServiceName} {
  Future<bool> initialize();
  bool get isInitialized;
  Future<void> dispose();
}
```

所有服务必须：
1. 实现 `initialize()` 方法
2. 提供 `isInitialized` getter
3. 实现 `Disposable` 接口（如需要资源清理）

### 5. 测试规范

#### 测试文件组织

```
test/
├── core/
│   ├── services/
│   │   ├── service_locator_test.dart
│   │   └── notification_service_test.dart
│   └── interfaces/
├── plugins/
│   └── {plugin-name}_test.dart
└── integration_test/
```

#### 测试命名

- 文件名: `{filename}_test.dart`
- 测试组: `describe('{ClassName}', () {`
- 测试用例: `test('should {expected behavior}', () {`

### 6. 文档规范

#### 代码注释

```dart
/// 单行注释（用于公共 API）
///
/// 多行描述
/// 可以有多个段落
class MyClass {
  /// 方法说明
  ///
  /// 参数说明：
  /// - [param1] 参数1的说明
  /// - [param2] 参数2的说明
  ///
  /// 返回值说明
  ReturnType myMethod(Type param1, Type param2) {
    // TODO: 待办事项
  }
}
```

#### Markdown 文档

- 使用清晰的标题层级
- 代码块指定语言
- 添加适当的 emoji 增强可读性
- 提供示例代码
- 更新交叉引用

### 7. Git 提交规范

#### 提交信息格式

```
<type>(<scope>): <subject>

<body>

<footer>
```

**类型**:
- `feat`: 新功能
- `fix`: 修复 bug
- `docs`: 文档变更
- `style`: 代码格式（不影响功能）
- `refactor`: 重构
- `test`: 添加测试
- `chore`: 构建/工具变更

**示例**:
```
feat(platform-services): add audio service implementation

- Implement AudioServiceImpl with audioplayers package
- Add support for system sounds and music playback
- Include volume control and player pool management

Closes #123
```

### 8. 安全规范

#### 输入验证

- 所有用户输入必须验证
- 使用参数化查询避免 SQL 注入
- 验证文件路径，防止路径遍历

#### 权限管理

- 最小权限原则
- 敏感操作需要用户确认
- 正确处理权限拒绝

### 9. 性能规范

- 使用 `const` 构造函数减少重建
- 避免在 `build()` 方法中进行重计算
- 使用 `ListView.builder` 而非 `ListView` 处理长列表
- 正确使用 `setState()`，避免不必要的状态更新

### 10. 国际化规范

- 所有用户可见文本必须使用国际化
- 中文和英文双语支持
- 日期、数字格式本地化

```dart
// ❌ 错误
Text('Hello World')

// ✅ 正确
Text(AppLocalizations.of(context).helloWorld)
```

## 🚀 AI 编码助手使用指南

### 开始新任务前

1. **阅读规则**: 检查 `.claude/rules/` 目录
2. **了解结构**: 查看 `docs/MASTER_INDEX.md`
3. **检查规范**: 参考 `docs/guides/` 中的相关指南

### 创建新文件时

1. **判断位置**: 使用文件组织决策树
2. **检查命名**: 遵循命名规范
3. **添加文档**: 公共 API 必须有注释
4. **更新索引**: 如需要，更新相关索引文件

### 修改现有代码时

1. **保持风格**: 遵循现有代码风格
2. **添加测试**: 为新功能添加测试
3. **更新文档**: 同步更新相关文档
4. **提交规范**: 使用规范的提交信息

### 禁止行为

- ❌ 在根目录创建临时文件
- ❌ 跳过测试直接提交
- ❌ 硬编码用户可见文本
- ❌ 忽略代码检查警告
- ❌ 破坏现有 API 兼容性（除非必要且有文档说明）

## 📚 相关文档

- [文件组织规范](rules/FILE_ORGANIZATION_RULES.md) - **必须遵守**
- [插件开发指南](../docs/guides/internal-plugin-development.md)
- [平台服务用户指南](../docs/guides/PLATFORM_SERVICES_USER_GUIDE.md)
- [文档主索引](../docs/MASTER_INDEX.md)

---

**版本**: v1.0.0
**最后更新**: 2026-01-15
**维护者**: Flutter Plugin Platform 团队

---

> 💡 **提示**: 本文档是 AI 编码助手的最高规则。在执行任何任务前，都应该先查阅相关规则和文档。
