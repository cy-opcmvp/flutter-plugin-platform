name: plugin_cli
description: Scaffold, validate and pack plugin directories for the plugin platform v2.
publish_to: none
resolution: workspace
environment:
  sdk: ^3.10.0

dependencies:
  plugin_contracts:
    path: ../plugin_contracts
  plugin_sidecar:
    path: ../plugin_sidecar

dev_dependencies:
  lints: ^6.0.0
  test: ^1.26.0
