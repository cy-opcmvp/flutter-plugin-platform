name: plugin_devkit
publish_to: none
resolution: workspace
environment:
  sdk: ^3.10.0

dependencies:
  plugin_contracts:
    path: ../plugin_contracts
  plugin_runtime:
    path: ../plugin_runtime

dev_dependencies:
  lints: ^6.0.0
  test: ^1.26.0
