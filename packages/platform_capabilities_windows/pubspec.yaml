name: platform_capabilities_windows
description: Windows capability bindings - GDI-based screen capture and system paths.
publish_to: none
resolution: workspace
environment:
  sdk: ^3.10.0

dependencies:
  ffi: ^2.1.4
  image: ^4.7.2
  platform_capabilities:
    path: ../platform_capabilities
  plugin_contracts:
    path: ../plugin_contracts

dev_dependencies:
  lints: ^6.0.0
  test: ^1.26.0
