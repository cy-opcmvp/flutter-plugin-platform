name: plugin_sidecar
description: Desktop sidecar plugin installation, JSON-RPC framing and process supervision.
publish_to: none
resolution: workspace
environment:
  sdk: ^3.10.0

dependencies:
  crypto: ^3.0.0
  plugin_contracts:
    path: ../plugin_contracts

dev_dependencies:
  lints: ^6.0.0
  test: ^1.26.0
