name: platform_capabilities_macos
description: Stub capability bindings for macos - all capabilities report capability.unsupported.
publish_to: none
resolution: workspace
environment:
  sdk: ^3.10.0

dependencies:
  platform_capabilities:
    path: ../platform_capabilities

dev_dependencies:
  plugin_contracts:
    path: ../plugin_contracts
  lints: ^6.0.0
  test: ^1.26.0
